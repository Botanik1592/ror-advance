<% if @answer.errors.present? %>
  <% @answer.errors.full_messages.each do |error| %>
    $('.answers-errors').html('<%= j error %>');
  <% end %>
<% else %>
  $('.answers').append('<%= j render @answer %>');
  $('#new-answer-body').val(" ");
<% end %>
